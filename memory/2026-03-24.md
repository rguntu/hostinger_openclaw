-e 
- 2026-03-24: OpenClaw Docker wrapper script (/entrypoint.sh calling /hostinger/server.mjs) overwrites default models to standard Gemini if GEMINI_API_KEY is present. OpenRouter default config won't persist unless OPENROUTER_API_KEY is provided as an env var to the Docker container or server.mjs is modified.

# Memory - 2026-04-17

- **Configuration Update:** Performed a major refactor of `openclaw.json` to unify agent behavior. 
- **Model Routing:** Centralized model routing via OpenRouter; transitioned primary model to Gemini 3.1 Flash (Utility) for cost efficiency, with Claude 3.5 Sonnet and Qwen 3 as fallbacks.
- **Routing Fix:** Successfully bound `stock-researcher` agent to Telegram topic 13 in group -1003839355873, and disabled `requireMention` for that topic to ensure clean routing.
- **Persona Sync:** Shifted from symlinked file-based persona (`SOUL.md`) to a unified, centralized strategy to ensure all agents consistently use the [PLAN]/[ACTION]/[OBSERVATION]/[RESULT] formatting.
- **Cleanup:** Purged legacy `shared-configs` directory and `sync_configs.sh` scripts to maintain a clean workspace environment.
